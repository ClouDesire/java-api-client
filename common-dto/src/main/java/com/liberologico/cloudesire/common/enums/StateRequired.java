package com.liberologico.cloudesire.common.enums;

/**
 * Two-letter country codes for which a "state" is required for a valid address
 */
public enum StateRequired
{
    US, CA, AU, CN, MX, MY, IT
}
